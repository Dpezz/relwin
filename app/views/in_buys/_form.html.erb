<%= form_for([@buy,@in_buy]) do |f| %>
  <% if @in_buy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@in_buy.errors.count, "error") %> prohibited this in_buy from being saved:</h2>

      <ul>
      <% @in_buy.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :product_id %><br>
    <%= f.select :product_id, @products.map{ |c| [c.name, c.id] }, { include_blank: "Select Product" },
     { :class => 'form-control'} %>
  </div>
  <div class="form-group">
    <%= f.label :unit %><br>
    <%= f.number_field :unit, class: 'form-control' %>
  </div>
  <div class="modal-footer">
    <%= f.submit class: 'btn btn-primary' %>
  </div>
<% end %>
