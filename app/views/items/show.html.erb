<% content_for :title do %>Producto<% end %>
<% content_for :menu_sales do %>class ="active"<% end %>

<p id="notice"><%= notice %></p>

<h1 class="page-header">Venta <i class="fa fa-angle-right"></i> <small>Producto</small></h1>

<p>
  <strong>Sale:</strong>
  <%= @item.sale.id %>
</p>

<p>
  <strong>Product:</strong>
  <% if @item.product %><%= @item.product.name %><% end %>
</p>

<p>
  <strong>Unit:</strong>
  <%= @item.unit %>
</p>

<%= link_to 'Edit', edit_sale_item_path(@sale,@item) %> |
<%= link_to 'Back', sale_items_path(@sale) %>
